/**
 * templates/index.ts
 *
 * Exports all template components.
 *
 * Templates are page-level layouts that compose organisms and
 * molecules into complete page structures. They handle page routing
 * and high-level state management.
 *
 * @example
 * ```tsx
 * import { LandingPageTemplate } from '@/components/templates';
 * ```
 */

// Export templates as they are created
// This file will grow as templates are extracted from pages
export {};
