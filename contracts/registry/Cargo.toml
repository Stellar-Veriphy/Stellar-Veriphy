[package]
name = "registry"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { version = "27.0.3", features = ["alloc"] }

[dev-dependencies]
soroban-sdk = { version = "27.0.3", features = ["alloc", "testutils"] }
